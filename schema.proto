message Vertex {
    repeated Edge edges = 1;
    map<string, bytes> metadata = 2;
    optional bytes content = 3;
}

message Edge {
    required uint64 ref = 1;
    optional bytes feed = 2;
    required string label = 3;
    map<string, bytes> metadata = 4;
}
